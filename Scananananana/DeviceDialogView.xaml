<Window x:Class="Scananananana.DeviceDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DeviceDialogView" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <ListView Grid.Row="0" Name="Devices" SelectedItem="Device" DisplayMemberPath="DeviceID"  />
        <Button Grid.Row="1" Width="100" Height="30" Name="Connect" Content="Connect" />
    </Grid>
</Window>
